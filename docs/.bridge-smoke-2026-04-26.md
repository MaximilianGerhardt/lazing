# Phase TB Smoke Marker

This file exists to verify that the post-commit bridge hook fires and
forwards a `commented` / `kind=git-commit` event to the lazyos-Workspace-Chat.

If you see a "Commit … " toast in the chat right after this commit, the
bridge is live.
