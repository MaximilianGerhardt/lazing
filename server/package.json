{
  "name": "lazyos-agent-server",
  "version": "0.2.0",
  "private": true,
  "description": "lazyOS Agent Backend — orchestrates Claude Code CLI per workspace (MAX-Plan, no API key). Port 4201.",
  "main": "agent-server.ts",
  "scripts": {
    "start": "tsx agent-server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
