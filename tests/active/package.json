{
  "name": "lazyos-active-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Isolated dev-deps for the active smoke harness — does NOT touch the repo's root package.json.",
  "scripts": {
    "test:ui": "playwright test --config playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
